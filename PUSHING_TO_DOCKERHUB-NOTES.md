# Build and Push Automation

The entire process (downloading custom libs, building, tagging, and pushing) is automated in the `build_and_push.sh` script.

## Prerequisites
- `gh` CLI installed and authenticated.
- `podman` installed.

## Usage

```bash
./build_and_push.sh
```
